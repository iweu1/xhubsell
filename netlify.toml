[build]
  command = "pnpm install && cd apps/api && npx prisma generate && cd ../.. && pnpm build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"
  NPM_FLAGS = "--legacy-peer-deps"
