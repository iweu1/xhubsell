# This is a root-level .env.example file for reference
# Each app (web and api) has its own .env.example file
# Copy those files to .env in the respective directories

# Global configuration
NODE_ENV=development
